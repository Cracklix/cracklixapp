rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEVELOPMENT RULES: Allowing all reads/writes to resolve unblocking failures.
    // WARNING: These should be hardened before production release.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}